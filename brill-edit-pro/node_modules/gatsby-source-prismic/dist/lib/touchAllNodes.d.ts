import * as RTE from 'fp-ts/ReaderTaskEither';
import { Dependencies } from '../types';
declare type TouchAllNodesConfig = {
    exceptPrismicIds: string[];
};
/**
 * Touches all gatsby-source-prismic nodes using the environment's `getAllNodes`
 * and `touchNodes` functions.
 */
export declare const touchAllNodes: (config?: TouchAllNodesConfig) => RTE.ReaderTaskEither<Dependencies, never, void>;
export {};
